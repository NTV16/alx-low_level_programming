#include "main.h"

/**
  *mul -  multiplies two integers, (int a) and (int b)
  *@a: first number to be multiplied
  *@b: operand two
  *Return: (int a * int b)
  */
int mul(int a, int b)
{
	return (a * b);
}
