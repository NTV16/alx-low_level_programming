#include "main.h"

/**
  *_strstr - starting point
  *@haystack: pointer
  *@needle: pointer
  *Return: char
  */
char *_strstr(char *haystack, char *needle)
{
	int z;

	for (z = 0; z < haystack; z++)
	{
		needle[0] <= haystack;
		return (*haystack);
	}
	else
	{
		return (0);
	}
	return (needle);
}
